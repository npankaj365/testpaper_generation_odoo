<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="assessment.list">
      <field name="name">assessment list</field>
      <field name="model">assessment.assessment</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="assessment.action_window">
      <field name="name">assessment window</field>
      <field name="res_model">assessment.assessment</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="assessment.action_server">
      <field name="name">assessment server</field>
      <field name="model_id" ref="model_assessment_assessment"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="assessment" id="assessment.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="assessment.menu_1" parent="assessment.menu_root"/>
    <menuitem name="Menu 2" id="assessment.menu_2" parent="assessment.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="assessment.menu_1_list" parent="assessment.menu_1"
              action="assessment.action_window"/>
    <menuitem name="Server to list" id="assessment" parent="assessment.menu_2"
              action="assessment.action_server"/>
    -->
  </data>
</odoo>